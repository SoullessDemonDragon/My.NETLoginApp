@{
    ViewData["Title"] = "LoginByUserName";
}

@model UserAuthCaller.DTO.UserGetByUserNameDto

<div class="container">
    @if (ViewBag.ToastMessage != null)
    {
        <div class="alert alert-danger alert-dismissible fade show" role="alert">
            @ViewBag.ToastMessage
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
    }

    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="card mt-5">
                <div class="card-body">
                    <h2 class="card-title text-center">Login</h2>

                    @using (Html.BeginForm("LoginByUserName", "Login", FormMethod.Post, new { @class = "mt-4" }))
                    {
                        @Html.AntiForgeryToken()

                        <div class="form-group">
                            <label for="userName" class="form-label mt-4"><i class="bi bi-person-circle fs-3"></i></label>
                            <div class="form-floating mb-3">
                                <input type="text" class="form-control" id="userName" name="userName" required />
                                <label for="userName">User Name</label>
                            </div>
                        </div>

                        <div class="form-group">
                            <label for="password" class="form-label mt-4"><i class="bi bi-key-fill fs-3"></i></label>
                            <div class="form-floating mb-3">
                                <input type="password" class="form-control" id="password" name="Password" required />
                                <label for="password">Password</label>
                            </div>
                        </div>
                        <div class="form-group">
                            <label class="form-label mt-4"></label>
                            <button type="submit" class="btn btn-primary btn-block">Login</button>
                        </div>
                    }
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}


